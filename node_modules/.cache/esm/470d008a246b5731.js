let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_246‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_246‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_246‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_246‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_246‍.w("./api",[["default",["api"],function(v){api=v}]]);_246‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);// dotenv를 불러와 config() 함수를 호출
// Node.js 에서 환경변수는 process.env 값을 통해 조회 가능
require('dotenv').config();








// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
// esLint 에서 process를 globals로 설정
const { PORT, MONGO_URI } = process.env;
// const { PORT } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _246‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// router setting
router.use('/api', api.routes()); // api 라우터

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
// 미들 웨어 적용 -> 라우터 전에
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않으면 4000 사용
const port = PORT || 4000;
app.listen(port, () => {
  _246‍.g.console.log('Listening to port %d', port);
});
